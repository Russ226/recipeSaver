##instance provider for the web layer

class Provider:

    @classmethod
    def createRecipeDAL(cls):
        ## read json db file

        ## add cursosr class

        ## return instance of the recipeDAL layer
        return 